<%= render(Common::Dropdown::Component.new(position: 'left')) do |c| %>
  <% c.with_button %>
  <%= link_to "edit", edit_comment_reply_path(reply.comment, reply), class: "text-violet-500 menu-item" %>
  <%= button_to "delete", comment_reply_path(reply.comment, reply), method: :delete, data: { turbo_confirm: "Are you sure you want to delete this comment?" }, form_class: "w-full", class: "text-red-500 menu-item" %>
<% end %>