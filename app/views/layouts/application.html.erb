<!DOCTYPE html>
<html class="scroll-smooth">
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="flex flex-col bg-zinc-900 text-zinc-100 w-full min-h-screen">
    <% if notice || alert %>
      <aside class="fixed top-32 border rounded-lg right-6 p-4">
        <% if notice %>
          <p class="text-green-500"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="text-red-500"><%= alert %></p>
        <% end %>
      </aside>
    <% end %>
    <%= turbo_frame_tag("modal") %>
    <%= render(partial: "partials/confirm_modal") %>
    <%= render(partial: "partials/header") %>
    <main class="flex flex-col flex-1 mx-auto w-full px-8 lg:px-0 lg:max-w-[85vw] 2xl:max-w-[65vw]">
      <%= yield %>
    </main>
    <footer class="flex justify-center p-12">
      <p class="text-zinc-300">
        © 
        <%= Time.now.year %>
        Blog</p>
    </footer>
  </body>
</html>
