<section class="relative flex flex-col gap-16 flex-1 w-full max-w-3xl">
  <hgroup class="flex flex-col gap-4">
    <h1 class="text-2xl font-medium">
      Edit post
    </h1>
    <p class="text-zinc-500">
      Make changes to your post. Post still is not visible to others. You can publish it later.
    </p>
  </hgroup>
  <%= render(partial: 'form', locals: { post: @post }) %>
</section>
