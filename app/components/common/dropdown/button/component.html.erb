<%= button_tag(
  class: "#{class_name} flex select-none rounded-lg p-2 transition-colors hover:bg-zinc-700 aria-expanded:bg-zinc-700",
  data: { "custom-dropdown-target": "button" },
  name: name || "dropdown_button",
) do %>
  <% if content.present? %>
    <%= content %>
  <% else %>
    <%= icon("more-horizontal", title: "More options", class_name: " w-5 h-5 text-zinc-300") %>
  <% end %>
<% end %>