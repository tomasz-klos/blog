<%= button_tag(
  class: "text-sm font-medium text-zinc-100 px-3 py-1 rounded-md transition hover:text-zinc-300 disabled:text-zinc-600 disabled:cursor-not-allowed #{class_name}",
  data: { 
    "action": "click->tabs#change",
    "tabs-target": "tab",
  },
  disabled: disabled,
  id: id,
  type: "button"
) do %>
  <%= content %>
<% end %>