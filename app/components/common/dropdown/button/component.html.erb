<%= button_tag(
  class: "flex select-none rounded-lg p-2 transition-colors hover:bg-zinc-800 aria-expanded:bg-zinc-800 #{class_name}",
  data: { "custom-dropdown-target": "button" },
  name: name || "dropdown_button",
) do %>
  <% if content.present? %>
    <%= content %>
  <% else %>
    <%= icon("more-horizontal", title: "More options", class_name: " w-5 h-5 text-zinc-300") %>
  <% end %>
<% end %>